server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/chathu-2003/Autometed_GreenHouse_Management_System
          default-label: main